buildscript {
    if (project == rootProject) {
        repositories {
            google()
            mavenCentral()
            jcenter()
        }

        dependencies {
            classpath 'com.android.tools.build:gradle:3.5.4'
        }
    }
}

apply plugin: 'com.android.library'

def safeExtGet(prop, fallback) {
    rootProject.ext.has(prop) ? rootProject.ext.get(prop) : fallback
}

android {
    compileSdkVersion safeExtGet('SdkMobileReactNative_compileSdkVersion', 31)
    defaultConfig {
        minSdkVersion safeExtGet('SdkMobileReactNative_minSdkVersion', 21)
        targetSdkVersion safeExtGet('SdkMobileReactNative_targetSdkVersion', 31)
        versionCode 1
        versionName "1.0"

    }

    buildTypes {
        release {
            minifyEnabled false
        }
    }
    lintOptions {
        disable 'GradleCompatible'
    }
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_9
        targetCompatibility JavaVersion.VERSION_1_9
    }
}

repositories {
    mavenLocal()
    maven {
        // All of React Native (JS, Obj-C sources, Android binaries) is installed from npm
        url("$rootDir/../node_modules/react-native/android")
    }
    google()
    mavenCentral()
    jcenter()
}

rootProject.allprojects {
    repositories {
      maven {
        name = "PRO"
        url = uri("https://facephicorp.jfrog.io/artifactory/maven-pro-fphi")
        credentials {
          username = System.getenv("USERNAME_ARTIFACTORY")
          password = System.getenv("TOKEN_ARTIFACTORY")
        }
        content {
                includeGroup "com.facephi.androidsdk"
                includeGroup "com.facephi.selphi.android"
                includeGroup "com.facephi.selphid.android"
                includeGroup "com.facephi.core"
                includeGroup "com.facephi.sdk"
        }
      }
      /*maven {
        name = "SNAPSHOT"
        url = uri("https://facephicorp.jfrog.io/artifactory/maven-snapshot-fphi")
        credentials {
          username = System.getenv("USERNAME_ARTIFACTORY")
          password = System.getenv("TOKEN_ARTIFACTORY")
        }
      }
      maven {
        name = "DEV"
        url = uri("https://facephicorp.jfrog.io/artifactory/maven-dev-fphi")
        credentials {
          username = System.getenv("USERNAME_ARTIFACTORY")
          password = System.getenv("TOKEN_ARTIFACTORY")
        }
      }*/
    }
}

dependencies {
  //noinspection GradleDynamicVersion
  api "com.facebook.react:react-native:+"  // From node_modules
  api "com.facephi.androidsdk:sdk:1.4.0"
  api "com.facephi.androidsdk:core:1.4.0"
  implementation "com.facephi.androidsdk:tracking_component:1.4.0"
  implementation "com.facephi.androidsdk:behavior_component:1.4.0"
  implementation "org.jetbrains.kotlin:kotlin-stdlib-jdk8:1.8.0"
  //implementation "com.facephi.core:licensingLite:1.0.0"
  //implementation "com.facephi.androidsdk:licensing_component:1.4.0"
  //implementation 'androidx.constraintlayout:constraintlayout:2.1.4'
  /*constraints {
    implementation("org.jetbrains.kotlin:kotlin-stdlib-jdk7:1.8.0") {
      because("kotlin-stdlib-jdk7 is now a part of kotlin-stdlib")
    }
    implementation("org.jetbrains.kotlin:kotlin-stdlib-jdk8:1.8.0") {
      because("kotlin-stdlib-jdk8 is now a part of kotlin-stdlib")
    }
  }*/
}
